node_modules/
.output/
dist/
.vite/
# Выхлоп `npm run build` для Vercel-пресета (~5 МБ) — генерируется, не хранится
.vercel/
.DS_Store
*.local

# Локальные секреты (ключи Gemini и пр.) — в гит не коммитим
.env
.env.*
!.env.example
