import { i as REVIEW_STATUSES, o as daysAgo, t as cn } from "./utils-DjTi_qin.js";
import { u as useStore } from "./router-B2dalpiV.js";
import { a as StatusBadge, i as PageHeader, t as EmptyState } from "./common-D0v8eWAw.js";
import { useState } from "react";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { Info } from "lucide-react";
import { toast } from "sonner";
//#region src/routes/review.tsx?tsr-split=component
var COLUMN_TONE = {
	"Ожидает проверки": "bg-warning",
	"На уточнении": "bg-primary",
	Подтверждено: "bg-danger",
	"Флаг снят": "bg-success"
};
function Review() {
	const { scopedPeople, setReviewStatus } = useStore();
	const [dragId, setDragId] = useState(null);
	const [over, setOver] = useState(null);
	const cases = scopedPeople.filter((p) => p.neet || p.neetReviewStatus !== "Флаг снят");
	return /* @__PURE__ */ jsxs("div", { children: [
		/* @__PURE__ */ jsx(PageHeader, {
			title: "Требуют внимания",
			subtitle: `Случаев в работе: ${cases.length}`
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "mb-5 flex items-start gap-3 rounded-xl border border-warning/30 bg-warning/10 p-4 text-sm",
			children: [/* @__PURE__ */ jsx(Info, { className: "mt-0.5 size-4 shrink-0 text-warning" }), /* @__PURE__ */ jsx("p", { children: "Флаг NEET — это сигнал для проверки уполномоченным сотрудником, а не окончательный административный статус." })]
		}),
		/* @__PURE__ */ jsx("div", {
			className: "grid gap-4 lg:grid-cols-4",
			children: REVIEW_STATUSES.map((col) => {
				const items = cases.filter((p) => p.neetReviewStatus === col);
				return /* @__PURE__ */ jsxs("div", {
					onDragOver: (e) => {
						e.preventDefault();
						setOver(col);
					},
					onDragLeave: () => setOver((o) => o === col ? null : o),
					onDrop: () => {
						if (dragId) {
							const person = cases.find((p) => p.id === dragId);
							setReviewStatus(dragId, col);
							toast.success(`Перемещено: ${col}`, { description: person?.fullName });
						}
						setDragId(null);
						setOver(null);
					},
					className: cn("rounded-xl border bg-muted/30 p-3 transition-colors", over === col ? "border-primary bg-primary/5" : "border-border"),
					children: [/* @__PURE__ */ jsxs("div", {
						className: "mb-3 flex items-center gap-2 px-1",
						children: [
							/* @__PURE__ */ jsx("span", { className: cn("size-2 rounded-full", COLUMN_TONE[col]) }),
							/* @__PURE__ */ jsx("h2", {
								className: "text-sm font-semibold",
								children: col
							}),
							/* @__PURE__ */ jsx("span", {
								className: "ml-auto rounded-full bg-card px-2 py-0.5 text-xs text-muted-foreground",
								children: items.length
							})
						]
					}), /* @__PURE__ */ jsxs("div", {
						className: "space-y-2",
						children: [items.length === 0 && /* @__PURE__ */ jsx(EmptyState, { text: "Пусто" }), items.slice(0, 40).map((p) => /* @__PURE__ */ jsxs("article", {
							draggable: true,
							onDragStart: () => setDragId(p.id),
							onDragEnd: () => setDragId(null),
							className: cn("cursor-grab rounded-xl border border-border bg-card p-3 active:cursor-grabbing", dragId === p.id && "opacity-50"),
							children: [
								/* @__PURE__ */ jsx("div", {
									className: "text-sm font-medium",
									children: p.fullName
								}),
								/* @__PURE__ */ jsxs("div", {
									className: "mt-0.5 text-xs text-muted-foreground",
									children: [
										p.age,
										" лет · ",
										p.mahalla
									]
								}),
								/* @__PURE__ */ jsxs("div", {
									className: "mt-2 flex items-center justify-between gap-2",
									children: [/* @__PURE__ */ jsx(StatusBadge, { status: p.status }), /* @__PURE__ */ jsxs("span", {
										className: "text-xs text-muted-foreground",
										children: [daysAgo(p.lastUpdate), " дн."]
									})]
								}),
								/* @__PURE__ */ jsx(Link, {
									to: "/person/$id",
									params: { id: p.id },
									className: "mt-2 inline-block text-xs font-medium text-primary hover:underline",
									children: "Открыть профиль →"
								})
							]
						}, p.id))]
					})]
				}, col);
			})
		})
	] });
}
//#endregion
export { Review as component };
